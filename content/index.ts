/**
 * Page & site copy lives in this folder.
 *
 * Edit the file that matches the page you want to change:
 *   site.ts      — nav, footer, brand, membership forms
 *   home.ts      — homepage
 *   about.ts     — About
 *   events.ts    — Events
 *   families.ts  — Families page intro (family bios → app/families/families-data.json)
 *   officers.ts  — Officers UI labels (names/bios → app/officers/officers-data.json)
 *   gallery.ts   — Gallery (albums → app/gallery/gallery-data.json)
 *   archive.ts   — Gallery archive (albums → app/gallery/archive/archive-data.json)
 *   alumni.ts    — Alumni intro (rosters → app/alumni/alumni-data.json)
 */

export { siteContent } from "./site"
export { homeContent } from "./home"
export { aboutContent } from "./about"
export { eventsContent } from "./events"
export { familiesContent } from "./families"
export { officersContent } from "./officers"
export { galleryContent } from "./gallery"
export { archiveContent } from "./archive"
export { alumniContent } from "./alumni"
