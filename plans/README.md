# Implementation Plans

Generated by the improve skill on 2026-08-21. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

> **Advisor note**: `Planned at` SHAs were left as `UNVERIFIED` because the operator
> disallows git commands without explicit permission. Before starting any plan,
> run `git rev-parse --short HEAD`, replace `UNVERIFIED` in that plan’s header,
> and use that SHA in the drift check. If in-scope files already differ from the
> plan’s “Current state” excerpts, STOP.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add typecheck script, Vitest baseline, and CI gates | P1 | M | — | DONE |
| 002  | Keep sealed family roster off the client until reveal | P1 | M | 001 (tests for gate helpers preferred) | DONE |
| 003  | Fix alumni `famhead,` role typos | P2 | S | — | DONE |
| 004  | Make officer bio cards keyboard-accessible | P2 | S | — | DONE |
| 005  | Fix theme toggle to use resolvedTheme | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first** — establishes `pnpm typecheck` and `pnpm test`. Plans 002–005 list
  those commands in Done criteria; if 001 is not done, substitute `pnpm build` for
  typecheck and skip test steps that require Vitest.
- **002 after 001** when possible — the Families server/client split benefits from
  a unit test on “sealed payload has no secrets.” 002 can still ship without 001
  if the operator accepts manual verification only.
- **003, 004, 005** are independent of each other and of 002; can run in parallel
  after or beside 001.

## Findings considered and rejected (this batch)

- Full JSON schema validation (Zod) for all data files — defer until after a
  test runner exists and a second bad-data incident; 003 is the immediate data fix.
- Gallery/archive list component merge — only worth it when both are already open.
- Prettier/EditorConfig — low pain for a small maintainer set.
- Moving `autoprefixer` to devDependencies — noise relative to this batch.
- Unused `animejs` / `date-fns` removal — valuable but not in the selected top 5.
- README/`content/` docs — valuable but not in the selected top 5.
